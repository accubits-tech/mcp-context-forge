# WARNING: These are placeholder credentials and MUST be changed for production.
# Generate new base64-encoded values with: echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  labels:
    app: postgres
type: Opaque
data:
  POSTGRES_USER: YWRtaW4=          # base64("admin") - change for production
  POSTGRES_PASSWORD: dGVzdDEyMw==  # base64("test123") - change for production
