# Copy keys you need to the deploy form's "Environment variables" section.
# Values entered there are encrypted at rest and injected only at container start.
# This file is for documentation only — it is not read by the running container.

# SAMPLE_GREETING=hello
